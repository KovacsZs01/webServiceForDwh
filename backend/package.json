{
  "name": "backend",
  "type": "module",
  "dependencies": {
    "dotenv": "^17.2.3",
    "express": "^5.2.1",
    "pg": "^8.16.3",
    "cors": "^2.8.5"
  },
  "scripts": {
    "dev": "nodemon --watch src --exec \"esbuild src/server.ts --bundle --platform=node --target=node22 --outdir=dist --format=esm --external:express --external:pg --external:dotenv && node dist/server.js\"",
    "build": "esbuild src/server.ts --bundle --platform=node --target=node22 --outdir=dist --format=esm --external:express --external:pg --external:dotenv",
    "start": "node dist/server.js"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/cors": "^2.8.12",
    "@types/node": "^25.0.1",
    "@types/pg": "^8.16.0",
    "esbuild": "^0.27.1",
    "nodemon": "^3.1.11",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  }
}